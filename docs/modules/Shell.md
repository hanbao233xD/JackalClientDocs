Shell
命令行
分类：Misc
描述：暂无描述

需求
- 安全级别：常规模块
- 权限需求：无
- 驱动依赖：否
- 联网需求：否
- 开发状态：稳定/常规
- 版本属性：普通可用

介绍
Shell（命令行）用于暂无描述。
适合在日常管理与自动化场景中按需启用。
初次使用可优先调整：Exclusive Mode、Batch File Execution Notify。

配置项
- Close Menu（中文：关闭菜单）：类型=布尔，默认=true
- Command Max Length（中文：命令最大长度）：类型=数值，默认=65535L
- Batch File Error Level（中文：批处理文件错误等级）：类型=枚举，默认="Only Exception"
  可选：Ignore（忽略）；Only Exception（仅异常）；All Errors（所有错误）
- Input Prompt（中文：输入提示）：类型=枚举，默认="$>"
  可选：>（未收录）；$>（未收录）；$（未收录）；>>>（未收录）；JC >（未收录）；Custom（自定义）
- Jackal Pro Subtheme（中文：Jackal Pro 子主题）：类型=枚举，默认="Simple"
  可选：Simple（简易）；Kali Hacker Box（Kali 黑客框）；Classic Kali（经典 Kali）；PowerLevel Arrow（PowerLevel 箭头）；Predator Minimal（迷你猎手）；Cyber Neon Frame（赛博霓虹框）；Military Protocol（军事协议）；Default（默认）
- Custom Input Prompt（中文：自定义输入提示）：类型=文本，默认=">>>"
- Exclusive Mode（中文：独占模式）：类型=布尔，默认=false
- Batch File Execution Notify（中文：批处理文件执行是否通知）：类型=布尔，默认=true
- Command Highlighter（中文：命令高亮器）：类型=布尔，默认=true
- Command Hints（中文：命令提示）：类型=布尔，默认=true
- Using Builtin Variables（中文：使用内置变量）：类型=布尔，默认=true
- Echo Command（中文：回显命令）：类型=布尔，默认=false
- Save Command History（中文：保存命令历史）：类型=布尔，默认=true
- Input Color（中文：输入内容颜色）：类型=文本，默认="white"
- bind（中文：绑定）：类型=按键/复合，默认={{"Keybind", {VK_RMENU, 'S'}}}
- Input Prompt Color（中文：输入提示颜色）：类型=文本，默认="yellow"
- Using Environment Variables（中文：使用环境变量）：类型=布尔，默认=true
- Chatter Color（中文：弹幕颜色）：类型=枚举，默认="White"
  可选：Black（黑色）；Gray（灰色）；White（白色）；Red（红色）；Gold（金瓜子）；Yellow（黄色）；Green（绿色）；Dark Green（深绿色）；Aqua（淡蓝色）；Blue（蓝色）；Magenta（品红色）；Pink（粉色）；Purple（紫色）；Pink-Magenta（粉-紫组合）；Red-Orange（红-橙组合）；White-Gray（白-灰组合）；Aqua-Blue（浅蓝-蓝组合）；Aqua-Pink（淡蓝-浅粉组合）；Green-Red（红-绿组合）；Astrolfo（未收录）；Colorful（缤纷）；Rainbow（彩虹色）
- Theme（中文：主题）：类型=枚举，默认="Jackal Pro"
  可选：Default（默认）；Jackal Pro（未收录）；Cmd（未收录）；PowerShell（未收录）；VS2022 Dev PowerShell（未收录）；Metasploit（未收录）；Kali Linux（未收录）
- Max History Length（中文：最大历史长度）：类型=数值，默认=500
- Allow Terminate Shell Thread（中文：允许终止命令行线程）：类型=布尔，默认=false
- Title Color（中文：标题颜色）：类型=枚举，默认="Rainbow"
  可选：Black（黑色）；Gray（灰色）；White（白色）；Red（红色）；Gold（金瓜子）；Yellow（黄色）；Green（绿色）；Dark Green（深绿色）；Aqua（淡蓝色）；Blue（蓝色）；Magenta（品红色）；Pink（粉色）；Purple（紫色）；Pink-Magenta（粉-紫组合）；Red-Orange（红-橙组合）；White-Gray（白-灰组合）；Aqua-Blue（浅蓝-蓝组合）；Aqua-Pink（淡蓝-浅粉组合）；Green-Red（红-绿组合）；Astrolfo（未收录）；Colorful（缤纷）；Rainbow（彩虹色）
- Command Completion（中文：命令补全）：类型=布尔，默认=true

历史更新
无（HISTORY 中暂无明确记录）

备注
该模块可能受系统版本、权限级别、目标进程状态或安全软件策略影响；若功能未生效，优先检查管理员权限、驱动依赖、联网状态与系统兼容性。

相关命令
无

相关模块
- [Active (活动)](./Active.md)
- [AutoSpeak (自动讲述)](./AutoSpeak.md)
- [AntiMicrophone (反麦克风)](./AntiMicrophone.md)
- [Console (控制台)](./Console.md)
- [QuickCommand (快速命令)](./QuickCommand.md)
- [ForceTopmost (强制置顶)](./ForceTopmost.md)
- [MemeTrigger (梗触发)](./MemeTrigger.md)
- [Encryptor (加密器)](./Encryptor.md)

相关资料
无

